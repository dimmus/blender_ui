inc_drw_private += include_directories('.')

subdir('shaders')

dir_drw_gpencil = meson.current_source_dir()

src_drw_shaders_c += [
  join_paths(dir_drw_gpencil, 'gpencil_defines.h'),
  join_paths(dir_drw_gpencil, 'gpencil_shader_shared.h'),
]